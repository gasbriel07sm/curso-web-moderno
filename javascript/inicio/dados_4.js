/**
 * (=) é o comando de atribuição
 * para definirmos constante chammos com o prefixo (const) -> não é possível realizar alterações em constantes
 */
const a = 3;

// ....

// a += 10; >> erro pois não podemos alterar constantes

// ....

console.log(a);